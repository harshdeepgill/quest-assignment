import styled from "styled-components";

function Home() {
    return ( 
        <DIV>
            
        </DIV>
     );
}

export default Home;

const DIV = styled.div`
    height: calc(100vh - 4rem);
`